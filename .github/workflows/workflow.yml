on:
  workflow_dispatch:
    inputs:
      upstream_branch:
        description: 'upstream_banch'
        required: false
        default: 'master'
jobs:
  build-chain:
    runs-on: ubuntu-latest
    steps:
      - name: "echo inside this repo"
        run: |
          echo "echo inside dispatch DownStream";
          sleep 10;
